import { SectorDirective } from './sector.directive';

describe('SectorDirective', () => {
  it('should create an instance', () => {
    const directive = new SectorDirective();
    expect(directive).toBeTruthy();
  });
});
